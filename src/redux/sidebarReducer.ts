import { ActionsType, sidebarType } from "./store";

const initionState: sidebarType = {}

const sidebarReducer = (state = initionState, action: ActionsType) => {
  return state
}

export default sidebarReducer;